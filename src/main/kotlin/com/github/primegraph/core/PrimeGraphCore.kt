package com.github.primegraph.core

/**
 * Shared cross-package vocabulary for PrimeGraph generated Kotlin packages, so
 * that a graph has exactly one nominal type per concept no matter how many
 * generated packages it spans.
 *
 * Placeholder public surface so the project compiles before the shared
 * vocabulary is migrated here.
 */
public object PrimeGraphCore {
  public const val NAME: String = "primegraph-core"
}
